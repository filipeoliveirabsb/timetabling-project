class solver:

    def __init__(self, schedules):
        self.schedules = schedules

    def bestSchedule(schedules):
        print("Calculating")
        bestSchedule = []
        
        # percorrer as schedules geradas e compara-las considerando
        # os ajustes para o mes seguinte e a média semanal de cada 
        # empregado (se >= 40)
        
        return bestSchedule